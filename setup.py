from setuptools import setup, find_packages

setup(
    name="cat-dog-classifier",
    version="1.0.0",
    description="Cat vs Dog image classifier trained with PyTorch",
    author="Ayush Dubey",
    packages=find_packages(),
    python_requires=">=3.10",
)
